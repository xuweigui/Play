<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                 http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                 http://www.springframework.org/schema/security
                 http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <http auto-config="true" use-expressions="true" entry-point-ref="myEntryPoint">

        <intercept-url pattern="/IressTest**" access="isAuthenticated()"/>
        <intercept-url pattern="/login.jsp" access="permitAll"/>

            <!-- access denied page -->
        <access-denied-handler error-page="/403" />

        <form-login
            login-page="/login.jsp"
            default-target-url="/welcome"
            authentication-failure-url="/login?error"
            username-parameter="username"
            password-parameter="password"
            />
        <logout logout-success-url="/login?logout"  />
            <!-- enable csrf protection -->
        <!--<csrf/>-->
    </http>

    <beans:bean id="myEntryPoint" class="com.windrift.security.spring.MyAuthenticationEntryPoint">
        <beans:constructor-arg index="0" value="/views/login.jsp"/>
    </beans:bean>

        <!-- Select users and user_roles from database -->
    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query=
                                       "select first_name, last_name, emp_no from employees where concat_ws('.', first_name, last_name)=?"
                               />
        </authentication-provider>
    </authentication-manager>


</beans:beans>